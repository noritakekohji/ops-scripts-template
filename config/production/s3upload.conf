# production: IA + KMS 必須、archive モード
# KmsKeyId は本番 KMS の alias（事前作成・権限付与済みのこと）
Bucket               = my-ops-prod-backups
Prefix               = production
StorageClass         = STANDARD_IA
ServerSideEncryption = aws:kms
KmsKeyId             = alias/ops-prod
Mode                 = archive
