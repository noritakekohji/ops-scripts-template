# Defaults for Backup-Ami.ps1 / backup_ami.sh (shared, Windows + Linux).
# Per-environment overrides go in config/<env>/backup_ami.conf.
# CLI flags (-Region/-r, -RetentionDays/-d, ...) override these.

# Region             = ap-northeast-1
# RetentionDays      = 7
# MinIntervalMinutes = 5
# NoReboot           = true
# Wait               = false
