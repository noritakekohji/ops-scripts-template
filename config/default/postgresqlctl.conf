# postgresqlctl.conf  -  PostgreSQL ライフサイクル制御 設定
# 優先順位: CLI 引数 > このファイル > スクリプト既定値

# 待機タイムアウト秒（start/stop -w 時）
WaitTimeoutSec = 120

# -w オプションをデフォルトで有効にする場合は true
Wait = false

# ログファイルパス（空の場合はファイル出力なし）
LogFile =

# ログレベル: DEBUG / INFO / WARN / ERROR
LogLevel = INFO
