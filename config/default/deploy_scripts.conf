# Deploy-Scripts.ps1 / deploy_scripts.sh の設定ファイル（PS / Bash 共有）
# CLI で -PathList / -L が渡された場合はそちらが優先される。
PathList = config/default/deploy_scripts.lst

# opt_root_dir  配備先 root（OS によって既定が異なるためコメントアウト）
# Backup        true なら既存ファイルをバックアップしてから上書き
# opt_root_dir = /opt/ops-scripts
# Backup       = true
