# service-wait — script-level settings only.
# Monitoring parameters (initial_wait_sec / interval_sec / success_threshold /
# timeout_sec / per_check_timeout_sec) live in the targets list (.lst) header.
# Any monitoring-key left here is ignored with a WARN.

# Log file (empty = console only). Path is created if needed.
LogFile  =
# DEBUG | INFO | WARN | ERROR
LogLevel = INFO
