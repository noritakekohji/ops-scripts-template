# get_server_info.conf
# Configuration for Get-ServerInfo.ps1 / get_server_info.sh
# CLI options always take priority over these settings.

# Default categories to collect (comma-separated, or "all")
# Category = all

# File logging (inherits from global.conf if not set here)
# LogFile  = C:\ProgramData\ops-scripts\logs\get_server_info.log
# LogLevel = INFO
