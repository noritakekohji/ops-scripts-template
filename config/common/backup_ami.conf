# Defaults for backup_ami.sh
# Per-environment overrides go in config/<env>/backup_ami.conf.
# CLI flags (-r, -d, ...) override these.

# Region             = ap-northeast-1
# RetentionDays      = 7
# MinIntervalMinutes = 5
# NoReboot           = true
# Wait               = false
