# Defaults for S3Upload.ps1 / s3upload.sh (shared)

# 既定のリストファイル（リポジトリ root からの相対パス、絶対パスでも可）。
# CLI で -PathList / -L が渡された場合はそちらが優先される。
PathList = config/common/s3upload.lst

# Bucket               = my-default-backup-bucket
# Prefix               = backups
# Region               = ap-northeast-1
# StorageClass         = STANDARD_IA
# ServerSideEncryption = AES256
# KmsKeyId             =
# Mode                 = archive
