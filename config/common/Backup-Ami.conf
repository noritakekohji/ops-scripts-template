# Defaults for Backup-Ami.ps1
# Per-environment overrides go in config/<env>/Backup-Ami.conf.
# CLI args (-Region, -RetentionDays, ...) override these.

# Region            = ap-northeast-1
# RetentionDays     = 7
# MinIntervalMinutes = 5
# NoReboot          = true
# Wait              = false
