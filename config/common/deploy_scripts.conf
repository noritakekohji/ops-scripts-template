# Deploy-Scripts.ps1 / deploy_scripts.sh の既定値（PS / Bash 共有）

# 既定のリストファイル（リポジトリ root からの相対パス、絶対パスでも可）。
# CLI で -PathList / -L が渡された場合はそちらが優先される。
PathList = config/common/deploy_scripts.lst

# OptRoot     配備先 root（OS によって既定が異なるためコメントアウト）
# IncludeEnvs 含める env: common / dev / staging / production / all（カンマ区切り可）
# Mode        既定 mode: script-only / with-config / with-tests / all
# Backup      true なら既存ファイルをバックアップしてから上書き

# OptRoot     = /opt/ops-scripts
# IncludeEnvs = common,production
# Mode        = with-config
# Backup      = true
