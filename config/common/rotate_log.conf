# Defaults for Rotate-Log.ps1 / rotate_log.sh (shared)

# 既定のリストファイル（リポジトリ root からの相対パス、絶対パスでも可）。
# CLI で -PathList / -L が渡された場合はそちらが優先される。
PathList = config/common/rotate_log.lst

# Pattern        = *.log
# MaxSizeMB      = 0
# MaxAgeDays     = 1
# Compress       = true
# RetentionCount = 30
# CopyTruncate   = false
