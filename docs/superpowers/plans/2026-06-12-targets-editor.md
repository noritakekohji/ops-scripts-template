# targets-editor (network-check 用 Excel エクスポートマクロ) + クリーンアップ 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** network-check の targets.lst を Excel で編集して UTF-8/LF でエクスポートできる .xlsm を追加し、tools/ 配下の作業残骸を掃除する。

**Architecture:** VBA ソース（targets-editor.bas）を真実の源として git 管理し、Excel COM を使う PowerShell ビルドスクリプトで .xlsm を生成する。テストは「マクロが出力する形式のサンプル lst」をフィクスチャとして既存パーサ（check_network_connectivity.sh）が受理することを bats で検証する（VBA 自体は自動テスト対象外）。

**Tech Stack:** VBA (ADODB.Stream), PowerShell 5.1 (Excel COM), bats, Pester

**Spec:** `docs/superpowers/specs/2026-06-12-tools-overhaul-design.md` セクション E + クリーンアップ

**前提知識（このリポジトリの規約）:**
- `.ps1` は **UTF-8 BOM 付き** で保存。`#Requires -Version 5.1` + `Set-StrictMode -Version Latest` 必須
- フィクスチャ等の共有テキストは **LF**。`.gitattributes` が強制する
- コミットは Conventional Commits（`feat:` / `chore:` / `test:` / `docs:`）
- テスト実行: `bash tests/run_unit.sh`（bats）/ `pwsh tests/run_unit.ps1`（Pester）。bats は Git Bash で動く

---

### Task 1: 作業残骸のクリーンアップと .gitignore 追記

**Files:**
- Modify: `.gitignore`（末尾に追記）
- Delete: `tools/perf-monitor/results_test/`、`tools/aws-instance-audit/__pycache__/`、`tools/server-compare/__pycache__/`

注意: `.gitignore` には未コミットの既存変更（`# === 共通最低限ルール (追記) ===` ブロック）がある。ユーザーが意図して追加したものなので、このタスクのコミットに**一緒に含めてよい**。

- [ ] **Step 1: .gitignore に追記**

末尾に以下を追加（`__pycache__/` は既存エントリがあるので追加しない）:

```gitignore

# --- perf-monitor local test sessions ---
tools/perf-monitor/results_test/
```

- [ ] **Step 2: 残骸ディレクトリを削除**

```powershell
Remove-Item -Recurse -Force tools/perf-monitor/results_test
Remove-Item -Recurse -Force tools/aws-instance-audit/__pycache__
Remove-Item -Recurse -Force tools/server-compare/__pycache__
```

- [ ] **Step 3: git status で消えたことを確認**

Run: `git status --short`
Expected: `?? tools/perf-monitor/results_test/` などが表示されなくなる。`M .gitignore` のみ残る。

- [ ] **Step 4: コミット**

```powershell
git add .gitignore
git commit -m "chore: ignore perf-monitor local test sessions, remove stray __pycache__/results_test"
```

---

### Task 2: エクスポート形式のフィクスチャと受理テスト

マクロが出力する形式の「契約」をフィクスチャとして固定し、既存パーサが受理することを検証する。データファイルなので TDD の red フェーズはなし。

**Files:**
- Create: `tests/fixtures/targets_editor_export_sample.lst`
- Create: `tests/bats/targets_editor.bats`

- [ ] **Step 1: フィクスチャを作成**

`tests/fixtures/targets_editor_export_sample.lst` を **UTF-8 BOM なし + LF** で作成。内容は Task 3 の VBA `BuildContent` が生成するものと一字一句同じ形式にする:

```
# Network connectivity check target list
# Generated by targets-editor.xlsm
#
# 4-field format: <host>, <port>, <expected>, <description>
#   expected: ok (expect reachable) / ng (expect unreachable) / - (no eval)
#   port:     TCP port number, or '-' for ping-only

# ---- Local ----
127.0.0.1, -, ok, Localhost ping
127.0.0.1, 65535, ng, Unused high port (should be closed)

# ---- No eval ----
127.0.0.1, -, -, Localhost (no evaluation)
```

PowerShell で LF/BOM なしを保証して書く場合:

```powershell
$content = @'
# Network connectivity check target list
# Generated by targets-editor.xlsm
#
# 4-field format: <host>, <port>, <expected>, <description>
#   expected: ok (expect reachable) / ng (expect unreachable) / - (no eval)
#   port:     TCP port number, or '-' for ping-only

# ---- Local ----
127.0.0.1, -, ok, Localhost ping
127.0.0.1, 65535, ng, Unused high port (should be closed)

# ---- No eval ----
127.0.0.1, -, -, Localhost (no evaluation)
'@ -replace "`r`n", "`n"
[IO.File]::WriteAllText("tests/fixtures/targets_editor_export_sample.lst", $content + "`n", (New-Object System.Text.UTF8Encoding($false)))
```

- [ ] **Step 2: bats テストを作成**

`tests/bats/targets_editor.bats`（既存 `network_check.bats` と同じ流儀）:

```bash
#!/usr/bin/env bats
# targets-editor の出力形式契約テスト:
# エクスポートされる lst が check_network_connectivity.sh で受理されること

load test_helper

CTL="${TOOLS_DIR}/network-check/check_network_connectivity.sh"
FIXTURE="${BATS_TEST_DIRNAME}/../fixtures/targets_editor_export_sample.lst"

@test "targets-editor: fixture は CRLF を含まない" {
    ! grep -q $'\r' "$FIXTURE"
}

@test "targets-editor: fixture は UTF-8 BOM を含まない" {
    [ "$(head -c 3 "$FIXTURE")" != $'\xef\xbb\xbf' ]
}

@test "targets-editor: fixture を network-check パーサが受理する" {
    if ! command -v ping >/dev/null; then skip "ping required"; fi
    run timeout 60 bash "$CTL" -l "$FIXTURE" -c 1 -t 2
    # 127.0.0.1 のみなので OK(0) または Warning(1) を許容
    [ "$status" -le 1 ]
}
```

- [ ] **Step 3: テスト実行**

Run（Git Bash）: `bats tests/bats/targets_editor.bats`
（bats が PATH にない場合は `bash tests/run_unit.sh` で全体実行し、targets_editor の 3 件が PASS することを確認）
Expected: 3 PASS

- [ ] **Step 4: コミット**

```powershell
git add tests/fixtures/targets_editor_export_sample.lst tests/bats/targets_editor.bats
git commit -m "test: add export-format contract fixture for targets-editor"
```

---

### Task 3: VBA ソース targets-editor.bas

**Files:**
- Create: `tools/network-check/targets-editor.bas`

VBA は自動テスト対象外。フィクスチャ（Task 2）と同一形式を出力するよう `BuildContent` を書く。

- [ ] **Step 1: targets-editor.bas を作成**

ファイルは **CRLF** で保存する（VBA エディタへのインポート用。`.gitattributes` が `*.bas` を扱わない場合はそのままでよい）。内容全文:

```vb
Attribute VB_Name = "TargetsEditor"
Option Explicit

' ============================================================================
' targets-editor  -  Export "Targets" sheet to network-check targets.lst
'
' Sheet layout (data starts at row 2):
'   A: Section   B: Host   C: Port   D: Expected   E: Description
'
' Output contract:
'   - UTF-8 without BOM, LF line endings
'   - 4-field format: <host>, <port>, <expected>, <description>
'   - Section change emits a blank line + "# ---- <Section> ----" comment
' ============================================================================

Private Const SHEET_NAME As String = "Targets"
Private Const FIRST_DATA_ROW As Long = 2
Private Const COL_SECTION As Long = 1
Private Const COL_HOST As Long = 2
Private Const COL_PORT As Long = 3
Private Const COL_EXPECTED As Long = 4
Private Const COL_DESC As Long = 5

Public Sub ExportTargets()
    Dim ws As Worksheet
    On Error Resume Next
    Set ws = ThisWorkbook.Worksheets(SHEET_NAME)
    On Error GoTo 0
    If ws Is Nothing Then
        MsgBox "Sheet '" & SHEET_NAME & "' not found.", vbCritical, "targets-editor"
        Exit Sub
    End If

    Dim lastRow As Long
    lastRow = ws.Cells(ws.Rows.Count, COL_HOST).End(xlUp).Row
    If lastRow < FIRST_DATA_ROW Then
        MsgBox "No data rows (row 2 and below are empty).", vbExclamation, "targets-editor"
        Exit Sub
    End If

    Dim errs As String
    errs = ValidateRows(ws, lastRow)
    If Len(errs) > 0 Then
        MsgBox "Validation failed. Fix the highlighted cells:" & vbCrLf & errs, _
               vbCritical, "targets-editor"
        Exit Sub
    End If

    Dim outPath As Variant
    outPath = Application.GetSaveAsFilename( _
        InitialFileName:="targets.lst", _
        FileFilter:="Target list (*.lst),*.lst,All files (*.*),*.*")
    If VarType(outPath) = vbBoolean Then Exit Sub   ' user cancelled

    WriteUtf8NoBomLf CStr(outPath), BuildContent(ws, lastRow)
    MsgBox "Exported: " & outPath, vbInformation, "targets-editor"
End Sub

' Returns "" when all rows are valid; otherwise a CRLF-joined error summary.
' Invalid cells get a light red interior; valid cells are reset.
Private Function ValidateRows(ByVal ws As Worksheet, ByVal lastRow As Long) As String
    Dim r As Long, errs As String
    Dim host As String, port As String, expected As String

    For r = FIRST_DATA_ROW To lastRow
        ws.Range(ws.Cells(r, COL_HOST), ws.Cells(r, COL_EXPECTED)).Interior.ColorIndex = xlNone

        host = Trim$(CStr(ws.Cells(r, COL_HOST).Value))
        port = Trim$(CStr(ws.Cells(r, COL_PORT).Value))
        expected = LCase$(Trim$(CStr(ws.Cells(r, COL_EXPECTED).Value)))

        If Len(host) = 0 Then
            ws.Cells(r, COL_HOST).Interior.Color = RGB(255, 200, 200)
            errs = errs & "Row " & r & ": Host is empty" & vbCrLf
        ElseIf InStr(host, ",") > 0 Then
            ws.Cells(r, COL_HOST).Interior.Color = RGB(255, 200, 200)
            errs = errs & "Row " & r & ": Host must not contain ','" & vbCrLf
        End If

        If Not IsValidPort(port) Then
            ws.Cells(r, COL_PORT).Interior.Color = RGB(255, 200, 200)
            errs = errs & "Row " & r & ": Port must be 1-65535 or '-'" & vbCrLf
        End If

        If expected <> "ok" And expected <> "ng" And expected <> "-" And Len(expected) > 0 Then
            ws.Cells(r, COL_EXPECTED).Interior.Color = RGB(255, 200, 200)
            errs = errs & "Row " & r & ": Expected must be ok / ng / -" & vbCrLf
        End If
    Next r

    ValidateRows = errs
End Function

Private Function IsValidPort(ByVal port As String) As Boolean
    If port = "-" Or Len(port) = 0 Then
        IsValidPort = True
    ElseIf IsNumeric(port) Then
        IsValidPort = (CDbl(port) >= 1 And CDbl(port) <= 65535 And CDbl(port) = Int(CDbl(port)))
    Else
        IsValidPort = False
    End If
End Function

' Builds the full file content. Lines are joined with LF only.
Private Function BuildContent(ByVal ws As Worksheet, ByVal lastRow As Long) As String
    Dim sb As String, r As Long
    Dim section As String, prevSection As String
    Dim host As String, port As String, expected As String, desc As String

    sb = "# Network connectivity check target list" & vbLf & _
         "# Generated by targets-editor.xlsm" & vbLf & _
         "#" & vbLf & _
         "# 4-field format: <host>, <port>, <expected>, <description>" & vbLf & _
         "#   expected: ok (expect reachable) / ng (expect unreachable) / - (no eval)" & vbLf & _
         "#   port:     TCP port number, or '-' for ping-only" & vbLf

    prevSection = Chr$(0)   ' sentinel: never equals a real section
    For r = FIRST_DATA_ROW To lastRow
        host = Trim$(CStr(ws.Cells(r, COL_HOST).Value))
        If Len(host) > 0 Then
            section = Trim$(CStr(ws.Cells(r, COL_SECTION).Value))
            port = Trim$(CStr(ws.Cells(r, COL_PORT).Value))
            expected = LCase$(Trim$(CStr(ws.Cells(r, COL_EXPECTED).Value)))
            desc = Trim$(CStr(ws.Cells(r, COL_DESC).Value))
            If Len(port) = 0 Then port = "-"
            If Len(expected) = 0 Then expected = "-"

            If section <> prevSection Then
                sb = sb & vbLf
                If Len(section) > 0 Then
                    sb = sb & "# ---- " & section & " ----" & vbLf
                End If
                prevSection = section
            End If
            sb = sb & host & ", " & port & ", " & expected & ", " & desc & vbLf
        End If
    Next r

    BuildContent = sb
End Function

' Writes content as UTF-8 WITHOUT BOM. ADODB's utf-8 charset always writes a
' 3-byte BOM, so re-read the text stream as binary from position 3 and save.
Private Sub WriteUtf8NoBomLf(ByVal filePath As String, ByVal content As String)
    Dim textStream As Object, binStream As Object
    Set textStream = CreateObject("ADODB.Stream")
    textStream.Type = 2                 ' adTypeText
    textStream.Charset = "utf-8"
    textStream.Open
    textStream.WriteText content

    textStream.Position = 0
    textStream.Type = 1                 ' adTypeBinary
    textStream.Position = 3             ' skip BOM (EF BB BF)

    Set binStream = CreateObject("ADODB.Stream")
    binStream.Type = 1
    binStream.Open
    textStream.CopyTo binStream
    binStream.SaveToFile filePath, 2    ' adSaveCreateOverWrite
    binStream.Close
    textStream.Close
End Sub
```

- [ ] **Step 2: フィクスチャとの形式一致を目視確認**

`BuildContent` のヘッダ 6 行とフィールド区切り（`", "`）が `tests/fixtures/targets_editor_export_sample.lst` と一致していることを確認する。

- [ ] **Step 3: コミット**

```powershell
git add tools/network-check/targets-editor.bas
git commit -m "feat(network-check): add targets-editor VBA source (targets.lst exporter)"
```

---

### Task 4: xlsm ビルドスクリプトと .xlsm 生成

**Files:**
- Create: `tools/network-check/build_targets_editor.ps1`（UTF-8 **BOM 付き** で保存）
- Create: `tools/network-check/targets-editor.xlsm`（スクリプトで生成して git 管理）

前提: Excel がインストールされ、Excel のオプションで「**VBA プロジェクト オブジェクト モデルへのアクセスを信頼する**」が有効なこと（無効だと VBProject アクセスで COM エラーになる）。Excel がない環境ではこのタスクの Step 2 をスキップし、README にビルド手順がある旨をユーザーに報告する。

- [ ] **Step 1: build_targets_editor.ps1 を作成**

```powershell
#Requires -Version 5.1
<#
.SYNOPSIS
    Build targets-editor.xlsm from targets-editor.bas via Excel COM.
.DESCRIPTION
    Creates the "Targets" sheet (headers, sample rows, validation, export
    button), imports the VBA module, and saves as .xlsm.
    Requires: Excel installed + "Trust access to the VBA project object model"
    enabled (File > Options > Trust Center > Macro Settings).
.NOTES
    Exit codes: 0=success, 5=build failure, 10=Excel/VBOM not available
#>
[CmdletBinding()]
param(
    [string]$OutputPath = (Join-Path $PSScriptRoot 'targets-editor.xlsm')
)
$ErrorActionPreference = 'Stop'
Set-StrictMode -Version Latest

$wb = $null
$basPath = Join-Path $PSScriptRoot 'targets-editor.bas'
if (-not (Test-Path $basPath)) {
    Write-Error "targets-editor.bas not found: $basPath" -ErrorAction Continue
    exit 5
}

try {
    $excel = New-Object -ComObject Excel.Application
} catch {
    Write-Host "[ERROR] Excel COM not available. Install Excel to build the xlsm." 
    exit 10
}

$excel.Visible = $false
$excel.DisplayAlerts = $false

try {
    $wb = $excel.Workbooks.Add()
    $ws = $wb.Worksheets.Item(1)
    $ws.Name = 'Targets'

    # --- headers ---
    $headers = @('Section', 'Host', 'Port', 'Expected', 'Description')
    for ($i = 0; $i -lt $headers.Count; $i++) {
        $cell = $ws.Cells.Item(1, $i + 1)
        $cell.Value2 = $headers[$i]
        $cell.Font.Bold = $true
        $cell.Interior.Color = 0xD9D9D9
    }
    $ws.Columns.Item(1).ColumnWidth = 18   # Section
    $ws.Columns.Item(2).ColumnWidth = 28   # Host
    $ws.Columns.Item(3).ColumnWidth = 8    # Port
    $ws.Columns.Item(4).ColumnWidth = 10   # Expected
    $ws.Columns.Item(5).ColumnWidth = 44   # Description
    $excel.ActiveWindow.SplitRow = 1
    $excel.ActiveWindow.FreezePanes = $true

    # --- sample rows (mirror tests/fixtures/targets_editor_export_sample.lst) ---
    $samples = @(
        @('Local',   '127.0.0.1', '-',     'ok', 'Localhost ping'),
        @('Local',   '127.0.0.1', '65535', 'ng', 'Unused high port (should be closed)'),
        @('No eval', '127.0.0.1', '-',     '-',  'Localhost (no evaluation)')
    )
    for ($r = 0; $r -lt $samples.Count; $r++) {
        for ($c = 0; $c -lt 5; $c++) {
            # NumberFormat @ keeps '-' and ports as literal text
            $ws.Cells.Item($r + 2, $c + 1).NumberFormat = '@'
            $ws.Cells.Item($r + 2, $c + 1).Value2 = $samples[$r][$c]
        }
    }

    # --- data validation: Expected = ok / ng / - (rows 2..500) ---
    $expectedRange = $ws.Range('D2:D500')
    $expectedRange.Validation.Add(3, 1, 1, 'ok,ng,-') | Out-Null   # xlValidateList
    $expectedRange.Validation.IgnoreBlank = $true
    $expectedRange.Validation.InCellDropdown = $true

    # --- data validation: Port = 1-65535 or '-' (custom formula; cells are text) ---
    $portRange = $ws.Range('C2:C500')
    $portFormula = '=OR(C2="-",C2="",AND(ISNUMBER(--C2),--C2>=1,--C2<=65535))'
    $portRange.Validation.Add(7, 1, 1, $portFormula) | Out-Null   # xlValidateCustom
    $portRange.Validation.IgnoreBlank = $true
    $portRange.Validation.ErrorMessage = "Port must be 1-65535 or '-'"

    # --- import VBA module (requires VBOM trust) ---
    try {
        $wb.VBProject.VBComponents.Import($basPath) | Out-Null
    } catch {
        Write-Host "[ERROR] Cannot access VBProject. Enable: File > Options >"
        Write-Host "        Trust Center > Macro Settings > 'Trust access to the"
        Write-Host "        VBA project object model', then re-run."
        exit 10
    }

    # --- export button ---
    $btn = $ws.Buttons().Add(380, 4, 120, 24)
    $btn.Text = 'Export targets.lst'
    $btn.OnAction = 'ExportTargets'

    # --- save as .xlsm (FileFormat 52 = xlOpenXMLWorkbookMacroEnabled) ---
    if (Test-Path $OutputPath) { Remove-Item -Force $OutputPath }
    $wb.SaveAs($OutputPath, 52)
    Write-Host "[OK] Built: $OutputPath"
    exit 0
} catch {
    Write-Host "[ERROR] Build failed: $($_.Exception.Message)"
    exit 5
} finally {
    if ($null -ne $wb)    { $wb.Close($false) }
    $excel.Quit()
    [void][Runtime.InteropServices.Marshal]::ReleaseComObject($excel)
}
```

保存時の注意: **UTF-8 BOM 付き**で保存すること（リポジトリ規約）。

- [ ] **Step 2: ビルド実行（Excel がある環境のみ）**

Run: `powershell -NoProfile -ExecutionPolicy Bypass -File tools/network-check/build_targets_editor.ps1`
Expected: `[OK] Built: ...\targets-editor.xlsm`、exit 0。
exit 10（VBOM 信頼未設定）の場合はメッセージの手順で Excel 設定を変更して再実行。
Excel がない環境ではスキップし、最終報告に「xlsm は未生成。Excel がある端末で build_targets_editor.ps1 を実行する」と明記する。

- [ ] **Step 3: 動作確認（Excel がある環境のみ・手動）**

生成された xlsm を開く → マクロ有効化 → サンプル 3 行のまま「Export targets.lst」ボタン → 任意の場所に保存 → 出力ファイルが `tests/fixtures/targets_editor_export_sample.lst` と diff 一致することを確認:

```powershell
git diff --no-index tests/fixtures/targets_editor_export_sample.lst <出力した targets.lst>
```

Expected: 差分なし。

- [ ] **Step 4: コミット**

```powershell
git add tools/network-check/build_targets_editor.ps1
git add tools/network-check/targets-editor.xlsm   # 生成できた場合のみ
git commit -m "feat(network-check): add targets-editor.xlsm build script and workbook"
```

---

### Task 5: ドキュメントと仕上げ

**Files:**
- Modify: `tools/network-check/README.md`（構成ツリーと新セクション）
- Modify: `CHANGELOG.md`（Unreleased > Added。未トラックなので初コミットになる。`VERSION` も同時に初コミットする）

- [ ] **Step 1: README.md 更新**

構成ツリー（README 冒頭のコードブロック）に 3 行追加:

```
├── targets-editor.xlsm             # targets.lst 編集用 Excel マクロブック
├── targets-editor.bas              # 上記の VBA ソース（真実の源）
├── build_targets_editor.ps1        # .bas から .xlsm を再生成するビルドスクリプト
```

末尾（終了コード表の後）に新セクションを追加:

```markdown
---

## targets.lst を Excel で作る（targets-editor.xlsm）

`targets-editor.xlsm` を開き、`Targets` シートに入力して
「Export targets.lst」ボタンを押すと 4-field 形式の targets.lst を出力します。

| 列 | 内容 |
|---|---|
| Section | 出力時に `# ---- <Section> ----` コメントになるグループ名（任意） |
| Host | ホスト名または IP（必須） |
| Port | TCP ポート番号、または `-`（Ping のみ）。空欄は `-` 扱い |
| Expected | `ok` / `ng` / `-`（ドロップダウン。空欄は `-` 扱い） |
| Description | 説明 |

- 出力は **UTF-8（BOM なし）+ LF**。リポジトリの LF 統一規約に準拠します
- 不正な入力（Host 空欄、Port 範囲外、Expected 不正）は赤くハイライトされ、
  修正するまでエクスポートされません

### マクロを修正するとき

真実の源は `targets-editor.bas` です。`.bas` を編集してから
`build_targets_editor.ps1` で `.xlsm` を再生成してください
（要 Excel + 「VBA プロジェクト オブジェクト モデルへのアクセスを信頼する」設定）。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build_targets_editor.ps1
```
```

- [ ] **Step 2: CHANGELOG.md 更新**

`## [Unreleased]` の `### Added` 直下に追記:

```markdown
- network-check: targets.lst 編集用の Excel マクロブック targets-editor.xlsm
  （VBA ソース targets-editor.bas + ビルドスクリプト build_targets_editor.ps1）
```

- [ ] **Step 3: テスト一式と BOM/CRLF 検査を実行**

Run: `bash tests/run_unit.sh`
Expected: 既存テスト + targets_editor.bats が PASS

Run（リポジトリの slash command 相当）: `.ps1` の BOM 検査
```powershell
$bytes = [IO.File]::ReadAllBytes("tools/network-check/build_targets_editor.ps1")[0..2]
($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF)
```
Expected: `True`

- [ ] **Step 4: コミット**

```powershell
git add tools/network-check/README.md CHANGELOG.md VERSION
git commit -m "docs(network-check): document targets-editor and update changelog"
```
